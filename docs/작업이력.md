# 나라장터 입찰정보시스템 — 작업 이력 (상세)

> 기록일: 2026-07-12 · 작업 기간: 2026-07-11 ~ 07-12
> 대상: `C:\Work\gadukcns` — 나라장터 입찰정보 수집·AI 분석·대시보드 시스템
> 발주처 도메인: **정보시스템(정보화/IT) 감리 및 컨설팅** 전문 회사 (건설 감리 아님)

이 문서는 사용자 지시별로 "요청 → 처리 → 결과/근거"를 시간순으로 정리한 것이다.

---

## 1. 하네스 클론 및 설치

**지시:** `https://github.com/revfactory/harness` 클론하고 설치(풀)

**처리:**
- `git clone` → `C:\Work\gadukcns\harness`
- Harness = Claude Code용 "팀 아키텍처 팩토리" 플러그인(메타 스킬). 도메인 한 문장 → 에이전트 팀 + 스킬 생성.
- 스킬 본체를 글로벌 설치: `~/.claude/skills/harness/` (SKILL.md + references 6종)

**결과:** 다음 세션부터 "하네스 구성해줘" 트리거로 사용 가능.

---

## 2. 하네스 구성 (에이전트 팀 + 스킬)

**지시:** 두 PDF 기반 하네스 구성
- `나라장터_입찰정보시스템_기능상세정의서_v1.0.pdf` (FR-01~15, 데이터모델 4장, RLS 6장, API 9장)
- `나라장터_입찰정보시스템_UIUX_스토리보드_v1.0.pdf` (S-01~13, 디자인 토큰)

**처리 (harness 6-Phase 워크플로우):**
- 실행 모드: **에이전트 팀** / 아키텍처: 파이프라인(스키마 우선) + 팬아웃(병렬) + 점진적 QA
- **에이전트 5** (`.claude/agents/`, 모두 `model: opus`):
  | 에이전트 | 역할 |
  |---|---|
  | data-architect | Supabase 스키마·RLS·pgvector (기반) |
  | collector-engineer | 나라장터 OpenAPI 수집 배치·첨부추출 |
  | ai-engineer | LLM 요약·bge-m3 임베딩·스코어링·인력매칭 |
  | frontend-engineer | Next.js 13화면·디자인토큰·실시간 |
  | qa-engineer (general-purpose) | 경계면 교차 검증·RLS 테스트 |
- **스킬 6** (`.claude/skills/`): supabase-data-layer, nara-collector, ai-enrichment, nextjs-screen-builder, integration-qa, **narajangteo-build**(오케스트레이터)
- `CLAUDE.md`에 하네스 포인터(트리거 규칙 + 변경 이력) 등록

**결과:** 구조 검증 통과 — commands 미생성, frontmatter 정상, 스킬-에이전트 연결 dead link 없음.

---

## 3. 시스템 구축 (에이전트 팀 실행)

**지시:** 나라장터 입찰정보시스템 구축해줘

**처리:** `narajangteo-build` 오케스트레이터로 팀 실행 (서브에이전트 모드, model=opus)
- data-architect(동기) → 계약 확정 → collector/ai/frontend(병렬) → qa(교차검증)

**생성 산출물:**
| 계층 | 파일 |
|---|---|
| DB | `supabase/schema.sql` — 12테이블·RLS 17정책·pgvector(1024)·`app_current_role()` |
| 수집 | `scripts/collect.mjs`, `scripts/attachments.mjs` |
| AI | `scripts/ai.mjs` (요약·임베딩·스코어링·매칭·daily_brief) |
| CI | `.github/workflows/collect.yml` (cron 07:00 KST) |
| 프론트 | `app/**` 13화면(S-01~13), `package.json`, `tailwind.config.ts` 등 |
| 감사추적 | `_workspace/01~05_*.md` |

검증: `tsc --noEmit` 통과, `next build` 16라우트, `.mjs` `node --check` 통과.

---

## 4. QA — 경계면 교차 검증 (2라운드)

**Round 1 — 확정 결함 5건:**
| # | 심각도 | 결함 | 수정 |
|---|---|---|---|
| 5a | CRITICAL | `users` RLS 부재 → anon 노출 + status 자가변경으로 권한상승(RLS 전체 우회) | users RLS + self_read/admin 정책, `app_current_role()`를 **SECURITY DEFINER**로 전환(잠김 회귀 방지) |
| 5b | CRITICAL | `rules` RLS 부재 → 스코어링 룰 임의 조작 | rules RLS(read active/write admin) |
| 4 | MAJOR | S-06 추천인력 필드명 불일치(`matched_members`↔`matches`) → 카드 영구 미렌더 | frontend를 `matches`로 통일 |
| 5c | MAJOR | `keyword_groups` write 정책 부재 → 그룹 생성 전원 차단 | owner 기반 write 정책 |
| 5d | MAJOR | 가격/이력/첨부 anon write 개방 | read-only RLS |

**Round 2:** FIX 5건 전부 PASS, 신규 회귀 0 → 통합 수정 완료.

---

## 5. 실 API 연동

**지시:** 나라장터 연결정보 제공
- Base: `https://apis.data.go.kr/1230000/ad/BidPublicInfoService`
- 서비스키(공공데이터포털)

**처리 (라이브 검증 `resultCode:00`):**
- 실제 op명·필드 실측 → 스펙 가정 정정:
  - 목록 op: `getBidPblancListInfoServcPPSSrch`(없음) → **`getBidPblancListInfoServc`**(용역)
  - `preprice_range`→`rsrvtnPrceRngBgnRate~EndRate`, `eval_base_amount`→`evlBssAmt`, `est_price`→`presmptPrce`
  - 변경이력 전용 op **없음**(API not found) → 목록 응답 `ntceKindNm='변경공고'` 등에서 파생
  - 첨부 전용 op **없음** → 목록 내장 `ntceSpecDocUrl1~10`+`ntceSpecFileNm1~10`
  - API 날짜범위 **월 1구간 제한**(3개월 요청 시 `resultCode:07`)
- `collect.mjs`/`attachments.mjs` 실측 반영, 없는 op 제거, `NARA_BID_TYPES` 유형선택 추가
- **환경변수 이름 통일**(`.env.local.example`): `NARA_API_KEY`→`NARA_SERVICE_KEY`, `SUPABASE_SERVICE_ROLE_KEY`→`SUPABASE_SERVICE_KEY`
- 하네스 메타(스킬/에이전트) op명 최신화, `_workspace/00_input/API_실측매핑.md` 작성
- 실 키를 `.env.local`(gitignore 보호)에 저장

---

## 6. Supabase 배포 + 라이브 검증

**지시:** Supabase URL + 키 제공 (`ykojtoyyhxoezkoxukgv.supabase.co`)

**처리:**
- 받은 `sb_publishable_...` = **클라이언트(anon) 키** → 프론트용 반영, service_role은 별도 필요 안내
- 스키마 적용 중 결함 2건 수정:
  - **42P01**: `app_current_role()`가 `users`보다 먼저 정의 → 함수를 모든 테이블 이후로 이동
  - **42P17**: `bids.status` 생성컬럼이 `current_date`(비immutable) 참조 → **일반 컬럼으로 전환** + `refresh_bids_status()` 함수(일 배치 RPC) + 프론트는 `deadline_dt`에서 실시간 파생
- 스키마 적용 성공 후 라이브 검증: 12테이블 생성, RLS 강제(anon read `[]`, anon write `401/42501`), `refresh_bids_status` RPC `204`

---

## 7. 데이터 수집

### 7-1. 테스트 수집 (service_role 키)
- 최근 3일·5건 바운드 수집 → bids 적재, 필드 매핑 정상, status 파생 정상, 가격은 미공개(정상)
- **삭제 지시** → 수집/파생 데이터(bids/prices/changes/attachments/daily_brief) + 커서 리셋. 계정·설정 보존.

### 7-2. 수집 날짜 확인
- 테스트 수집 = 등록일시 기준 최근 **3일**(오늘 하루 아님), 첫 5건.

### 7-3. 3개월 룰기반 백필
**지시:** 이 룰(정보시스템 감리·컨설팅) 기준으로 오늘 기준 3개월 수집
- 규모 분석: 3개월 용역공고 ≈ **52,928건**, 룰 관련 ≈ 6.8%
- 사용자 선택: **룰 관련만 + 룰 스코어링(LLM 없이), 가격 생략**
- `scripts/backfill_rules.mjs` 작성 → 월 3구간 페이징 → 룰 매칭 필터 → score/tags upsert
- **결과: 스캔 52,928 → 관련(고유) 2,858건 적재**, score≥5 163건

---

## 8. AI 요약 보강

**지시:** 상위 점수만 AI 보강
- `ai.mjs`에 `AI_MIN_SCORE` 필터 추가 → 점수≥5 상위 163건만 처리
- 실행 중 모델 ID 오류 발견: `anthropic/claude-3.5-haiku`(404) → OpenRouter 실측으로 **`anthropic/claude-haiku-4.5`** 로 정정
- **결과: enriched=163 failed=0** (요약 Markdown + bge-m3 1024 임베딩 + daily_brief)

---

## 9. 도메인 명확화

**지시:** 감리·컨설팅 → **정보시스템(정보화/IT) 감리 및 컨설팅**으로 명확히

**처리:**
- 스펙 근거: member_table `license_name=정보시스템감리사`, `specialty_field=감리/ISP/성과평가/보안`
- `CLAUDE.md`에 발주처 도메인·제외어(건설/건축/토목/소방/전기 감리) 규칙 명시
- 프로젝트 메모리 저장(`narajangteo-target-domain`)
- 정리: 수집은 용역 전체, 분야 타겟팅은 keyword_groups(FR-13)+rules 스코어링(FR-05)

---

## 10. 계정/인증

**지시:** 관리자 로그인 계정 생성 (`wiki@korea.kr` / `admin!@098`)
- Auth 유저 생성(email_confirm) + `public.users` 행(role=admin, status=active, dept=경영지원)
- `HMAC_KEY` 설정, email_hash = HMAC-SHA256
- **로그인 E2E 검증:** 토큰 발급 `200`, 프로필 role=admin, admin 전용 `app_settings` 접근 `200`

---

## 11. 프론트 UI 수정

| 지시 | 처리 | 파일 |
|---|---|---|
| 프론트 서버 구동 + 랜딩 | `npm install` + dev 서버(`localhost:3000`), 로그인 화면 캡처 | — |
| 로그인 첫 페이지 "나" 뱃지 삭제 | 배지 div 제거 | `app/(auth)/layout.tsx` |
| 통계 대시보드 최상단 + 로그인 후 랜딩 | 네비 순서 이동, 루트/로그인 리다이렉트 `/dashboard/stats` | `components/layout/nav.ts`, `app/page.tsx`, `app/(auth)/login/page.tsx` |
| 통계 대시보드 주력사업 설정 | 주력분야별 차트 추가, KPI 주력관점(전체 주력공고·진행중·고득점·관심), 총계 정확 count | `app/(app)/dashboard/stats/page.tsx` |
| 사이드바 "나" 뱃지 삭제 | 배지 div 제거 | `components/layout/Sidebar.tsx` |
| 사이드바 "입찰정보시스템" 가독성 | `text-lg font-extrabold tracking-tight text-primary` | `components/layout/Sidebar.tsx` |

---

## 12. 스코어링 규칙 / 재스코어링

**지시:** 스코어링 규칙 수정했는데 반영됐나? → 재스코어링 + S-12에 "재스코어링" 기능 추가

**처리:**
- 진단: `rules` 테이블은 수정 반영됨(활성 10개, 감리+ISP 중심), 그러나 기존 `bids.score`는 옛 규칙값 그대로 → **자동 재계산 트리거 없음**(의도된 설계)
- **재스코어링 실행** (`scripts/rescore.mjs`, service_role): 2,858건 재계산, 변경 2,724건 → **score>0: 2,858→168, score≥5: 163→63**
- **S-12 "재스코어링" 버튼 추가** (`app/(app)/admin/rules/page.tsx`): `rescore_bids()` RPC 호출, 로딩/결과/함수미배포 안내
- **DB 함수** `rescore_bids()`(SECURITY DEFINER, admin 전용) 작성 → `supabase/schema.sql` + `supabase/rescore_bids.sql`
- 통계 KPI/차트를 관련(score>0) 기준으로 수정 → 재스코어링 후 168건으로 정직 반영

---

## 13. DB 정리 (룰 기준 공고 정리)

**지시:** S-04에서 S-12 규칙 기준으로 DB 정리 기능 설계 → 구현
- **설계:** `docs/기능검토_룰기준_DB정리.md` (dry-run 필수·watchlist 보호·소프트 아카이브 권장)
- **구현:** `supabase/cleanup_bids.sql`(`archived_at` 컬럼 + `cleanup_log` + `cleanup_bids()`/`restore_bids()` 함수), `components/bids/CleanupPanel.tsx`(S-04 admin), 목록·통계·캘린더에 `archived_at is null` 필터
- **라이브 검증:** dry-run 2,453건 예측 → 아카이브 실행 **2,453건**(노출 2,858→405), 감사로그 기록. "정리 대상 0건"은 정상(룰 최소가중치 4라 score 1~3 부재 + 남은 score-0은 최근7일/AI보호)임을 파라미터 스윕으로 검증.

## 14. 첨부파일 기능

**지시:** 첨부 수집 여부 확인 → 정보 정규화 → 목록/상세 표시 → 차수별 그룹
- 첨부 URL·파일명은 `bids.raw`에 이미 존재. 실물 다운로드/추출은 미실행 확인.
- `scripts/extract_attachment_info.mjs` → `bid_attachments` **1,108행**(355공고, downloaded=false) 정규화
- **S-04·S-05:** 카드에 **📎 첨부 N** 배지 (`fetchBids`에서 개수 부착 + `BidCard`)
- **S-06:** 첨부 카드 신규 추가(원인: 조회·렌더 코드 부재였음) → doc_type·파일명·다운로드↗
- **다차수 중복:** 원인=다차수(bid_seq 000/001/002)가 bid_no 조회로 합쳐짐 → **차수별 그룹 + 최신 차수 우선** 렌더로 변경

## 15. 우선순위 발주기관 검토
**지시:** 주력사업 기준 자주 거래 발주기관 우선정렬+배경색 검토 → `docs/기능검토_발주기관_우선순위.md` (기존 `rules(type='org')` 재사용 권고)

## 16. 고객사 정보 수집·관리

**지시:** `gadukcns.com/41` 고객사 정보 수집·관리 기능상세정의서 작성 → 구현
- **라이브 수집:** (주)가덕씨엔에스 고객사 **39곳**(중앙부처13·지자체3·공공5·의료17·금융1) — 보건·의료 강세
- **정의서:** `docs/기능상세정의서_고객사관리_v1.0.md` (FR-16~20)
- **구현:** `supabase/clients.sql`(clients 테이블+RLS+`sync_client_org_rules()`), `scripts/seed_clients.mjs`(39곳)·`collect_clients.mjs`(FR-17 자동수집), **S-14 고객사 관리**(`admin/clients`), S-04/05 **고객사 우선정렬 + ⭐배지 + 배경강조(FR-18)**, 수요기관도 매칭
- **라이브 검증:** 39곳 시드 + org룰 39 동기화 + 재스코어링 → S-04에서 분당서울대병원·질병관리청(수요) 공고 상단·강조 확인

## 17. 통계 대시보드 v2 강화

**지시:** S-10을 강력한 한눈 현황 대시보드로 → 기능요구사항 + 구현
- **정의서:** `docs/기능요구사항_통계대시보드_v2.md` (DFR-01~11)
- **구현:** `lib/queries/stats.ts`(집계 병렬조회) + S-10 위젯 그리드 재구성 — KPI 10종·마감임박 액션·주력분야·고객사 현황(보건집중률)·스코어링 품질·유입/마감 추세·발주/수요기관·AI 브리핑·데이터 상태·드릴다운·실시간
- 차트 렌더: `ResponsiveContainer` 헤드리스 높이 경합 → **고정 픽셀 높이 + isAnimationActive false**로 해결(실브라우저 정상)

## 18. 현황 기준 = 스코어링 가중치 4이상

**지시:** S-10·S-08의 현황 기준을 점수≥4로
- **S-10:** compute에 `REL_MIN=4` base 집합 → 전 위젯이 score≥4 관련 공고 기준(진행중 144→49, 임박 48→27, 추정가 1,814→797.6억)
- **S-08 캘린더:** 조회에 `.gte("score",4)` 추가

## 19. 다크/화이트 모드

**지시:** 검토 → 구현
- **검토:** `docs/기능검토_다크화이트모드.md` (토큰 중앙화라 난이도 낮음)
- **구현:** `globals.css` `:root[data-theme="dark"]` 다크 변수 세트 + `color-scheme`, `components/ui/ThemeToggle.tsx`(헤더 ☀/🌙), `layout.tsx` FOUC 방지 스크립트, 차트 그리드선·input 토큰화(입력요소 다크 미적용 수정)
- 라이브 검증: S-04·S-08 다크 렌더(FullCalendar 포함) 확인

## 20. 온디맨드 AI 요약 생성·저장

**지시:** 요약 없으면 클릭 시 실시간 생성·저장
- `app/api/ai-summary/route.ts`(서버, service_key+OpenRouter, 세션검증) → 생성 후 `bids.ai_summary` 저장
- S-06 AI 브리핑에 **⚡ AI 요약 생성(실시간)** 버튼 → 성공 시 재조회로 표시
- 라이브 검증: 요약 없는 공고 호출 → `saved:True` 실요약 생성 확인

## 21. S-04 우선순위 정렬 조정

**지시:** 주력사업(1순위) → 고객사(2순위)
- `fetchBids` 재정렬: ① 주력사업 점수(`score_breakdown.base`−exclude) → ② 고객사 → ③ 최신
- 결과: org 이중가산만 높던 공고(분당서울대 총20, 주력0) 하락, **주력 강한 KTL ISP/ISMP(주력12)가 1위**

## 22. 기타 UI 수정
- 로그인/사이드바 "나" 뱃지 삭제, 사이드바 타이틀 가독성(text-lg extrabold)
- 통계 대시보드 최상단 이동 + 로그인 랜딩=`/dashboard/stats`
- S-10 마감임박 항목에 **(발주기관)** 프리픽스 표기

---

## 부록 A. 현재 데이터 상태 (2026-07-12 기준)
- `bids`: 2,858건 적재. **아카이브 2,453 / 노출 405**. 노출 중 관련(score≥4) **193건**, 고득점(≥5) **99건**, 고객사 공고 **47건**
- `clients`: **39곳**(우선). `rules`: 활성 **49개**(키워드 10 + org 39=고객사). `keyword_groups` 7
- AI 요약: 배치 163 + 온디맨드(요청 시). `daily_brief` 생성됨. `bid_attachments` 1,108행(정보만)
- `users`: 관리자 1 (wiki@korea.kr). `watchlist` 0. dev 서버 실행 중
- 적용된 SQL(SQL Editor): `schema.sql`·`rescore_bids.sql`·`cleanup_bids.sql`·`clients.sql`

## 부록 B. 남은 작업 (선택)
1. 정규 배치 가동: GitHub Secrets 등록 후 `collect.yml`(07:00 cron) 활성화
2. 첨부 **실물 추출**: `attachments.mjs`(kordoc 설치) → extracted_text → AI 요약 품질↑
3. S-13 API 키 저장·회원가입 insert = 서버(Edge Function) 연결
4. 고객사 org 매칭 **이중가산 방지**(공고당 1회) / 별칭 정리
5. 대시보드 전일대비 델타용 `dashboard_daily` 스냅샷(DFR-11), 전역 필터 바
6. 아카이브 잔여(2,453) 영구삭제 여부 결정(현재 소프트 보존)

## 부록 C. 보안 주의
- 공유 키(나라장터·Supabase service_role·OpenRouter) + DB 마스터/HMAC 키는 `.env.local`(gitignore)에만 존재, 소스 노출 없음.
- **운영 전환 시 키 재발급 권장**(대화 중 노출). 운영은 GitHub Secrets로만.

## 부록 D. 주요 생성/수정 파일
```
supabase/  schema.sql · rescore_bids.sql · cleanup_bids.sql · clients.sql
scripts/   collect.mjs · attachments.mjs · ai.mjs · backfill_rules.mjs · rescore.mjs
           seed_clients.mjs · collect_clients.mjs · extract_attachment_info.mjs
app/api/ai-summary/route.ts
app/(app)/dashboard/stats/page.tsx · admin/{rules,clients}/page.tsx · bids/[id]/page.tsx · calendar/page.tsx
app/(auth)/layout.tsx · login/page.tsx · app/page.tsx · app/layout.tsx · globals.css
components/  bids/{BidCard,CleanupPanel}.tsx · ui/ThemeToggle.tsx · layout/{Sidebar,TopBar,nav}.tsx
lib/  queries/{bids,stats}.ts · supabase/types.ts · design/dday.ts
tailwind.config.ts · package.json · .env.local(.example) · .gitignore
.claude/agents/*.md (5) · .claude/skills/*/SKILL.md (6) · CLAUDE.md
docs/  작업이력.md · 기능검토_룰기준_DB정리.md · 기능검토_발주기관_우선순위.md
       기능상세정의서_고객사관리_v1.0.md · 기능요구사항_통계대시보드_v2.md · 기능검토_다크화이트모드.md
```
